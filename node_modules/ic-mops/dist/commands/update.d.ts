type UpdateOptions = {
    verbose?: boolean;
    dev?: boolean;
    lock?: "update" | "ignore";
    major?: boolean;
};
export declare function update(pkg?: string, { lock, major }?: UpdateOptions): Promise<void>;
export {};
